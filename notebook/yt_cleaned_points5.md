- Welcome to this video course on Langraph, the powerful Python library for building advanced conversational AI workflows.
- In this course, Vbeca will teach you how to design, implement, and manage complex dialogue systems using a graph-based approach.
- By the end, you will be equipped to build robust, scalable conversational applications that leverage the full potential of large language models.
- Hey guys, my name is Vava and I am a robotics and AI student.
- In this course, we are going to be learning all about the fundamentals of Langraph.
- I assume you have heard of Langraph before, which is why you clicked on this course.
- I am also going to assume you have never coded in Langraph before.
- Because of that assumption, I have explained every single thing in as much detail as I possibly can.
- This may also mean that I go slowly at times.
- If you want, you can always speed me up.
- We are going to be building a lot of graphs and a lot of AI agents.
- We are going to be learning a lot about the theory, and I have also provided exercises throughout the course in which all of the answers will be provided on GitHub.
- If you are ready to start this journey with me, let us go to our first section.
- Welcome to the first section of this course.
- In this section, we will be covering something called type annotations.
- Admittedly, this is going to be a completely theoretical section, but it will be short and brief.
- The reason I have kept this section in the course is because when we eventually start coding our AI agents, graphs, and Langraph workflows, these annotations will appear everywhere.
- I do not want you to start coding without ever having seen them before or without really knowing what they are.
- Let us begin with dictionaries.
- Dictionaries are a data structure, but there is a reason I have kept them here.
- Here is how a dictionary is described in Python.
- In this case, I have described a very simple dictionary called `movie`.
- It has two keys, `name` and `year`.
- It has two values, `Avengers Endgame` and `2019`.
- Dictionaries are useful because they allow for efficient data retrieval based on unique keys.
- They are flexible and easy to implement.
- However, there is a potential problem with them.
- It is a challenge to ensure that the data has a particular structure.
- This can be a huge problem in larger projects.
- In simple terms, a dictionary does not really check whether the data has the correct type or structure.
- That can be the source of many logical errors in a project.
- If the project is very large, that kind of problem can be a headache to identify because it is such a small detail.
- The solution is something called a typed dictionary.
- Here is an example of how you create a typed dictionary in Python.
- This type annotation is used extensively in Langraph.
- It will be used to define states.
- Do not worry if we have not covered states yet.
- We will cover that in the next section.
- A typed dictionary is easy to implement.
- You implement it as a class.
- In this case, I have implemented the same example I showed in the previous section.
- It has the same exact keys and values.
- It still has `name` and `year`.
- Notice that in this class, I have defined the actual data type of what each key should be.
- For example, `name` is a string and `year` is an integer.
- To initialize the dictionary, I have done the exact same thing.
- It has `Avengers Endgame` and `2019`.
- There are two main benefits of using a typed dictionary.
- The first benefit is type safety because we have explicitly defined what should be in this data structure.
- That reduces runtime errors.
- The second benefit is readability, and that will also make debugging easier if something goes wrong within the typed dictionary.
- Now we move on to another type annotation, which is `Union`.
- You may have seen these later type annotations before if you have coded in Python, but I am only giving you a high-level overview of what they are.
- Consider this example.
- I have created a very simple function that takes in a value and squares it.
- In this case, the input `x` could be either an integer or a float.
- `Union` basically says that whatever value you have can be only these data types.
- In this case, `x` can only be an integer or a float.
- If I pass in `5` or `1.234`, this would be completely fine.
- It would square the number and everything would work.
- If I passed in a string like `I am a string`, it would fail completely.
- This function is quite easy, and if I passed in `I am a string`, it would have failed anyway.
- But in more complicated applications, you can hopefully see how useful this is.
- In fact, the makers of LangChain and Langraph used `Union` quite extensively when building the actual library.
- It is flexible and easy to code.
- It allows for type safety because it can provide hints to help catch incorrect usage.
- Something similar to `Union` is another type annotation called `Optional`.
- `Optional` is quite similar.
- In this case, I have described another function called `nice_message`.
- You pass in a name.
- If you pass in a name, it will say `hi there name`.
- For example, if the name is `Bob`, and I pass `Bob` to this function, it would say `hi there Bob`.
- But what if I do not pass anything?
- If I do not pass anything, `Optional`, because I have used it, says that the `name` parameter could either be a string or a `None` value.
- If I pass in nothing, it will go into this `if` statement and say `hey random person`.
- It is also important to emphasize that it cannot be anything else.
- It cannot be an integer, a boolean, a float, or anything like that.
- It has to be either a string or a `None` value because that is what I have defined here.
- Now comes another type annotation called `Any`.
- `Any` is the easiest one to understand.
- It literally means this value could be anything.
- It could be any data structure.
- In this case, I have created a simple function called `print_value`, where it takes in something and prints it.
- For example, I passed in this string and it prints it.
- Anything and everything is allowed.
- One last type annotation, I promise: the `lambda` function.
- `lambda` functions are quite useful.
- I will give you two examples.
- The first example is this really simple example.
- We already created a square function before, where it takes in a value, takes in a number, and squares it.
- For example, if I passed in `square(10)`, it would give me `100`.
- Let me give you a second example.
- If you come from a little bit of a code background, then you have probably seen or used `lambda` before, and you have definitely used `map` before because it is quite efficient.
- For example, if I pass in `1, 2, 3, 4`, what this code is saying is that it squares each number in `nums`.
- This `map` function maps each value and performs this function on it, `x * x`.
- That gives `1, 4, 9, 16`, and then converts that back into a list.
- `lambda` functions are just a shortcut to writing small functions, and they make everything quite efficient.
- This could have been done in one line as well, but a beginner programmer might have used a `for` loop, while a more advanced programmer could have used this, which is much more efficient.
- Hopefully you can start to see how powerful these type annotations are.
- These will keep coming up.
- You do not need to memorize them, you only need to have a high-level overview of what they are.
- We will continue in the next section.
- In this section, we will look at the different elements in Langraph.
- Let us begin with the first element, one of the most fundamental elements in all of Langraph: the state.
- A state is a shared data structure that holds the current information or context of the entire application.
- In simpler terms, it is like the application's memory, where it keeps track of the variables and data that nodes can access and modify as they execute.
- Do not worry if you do not understand what a node is yet.
- We will talk about that in the next slide.
- As a good analogy, think of the whiteboard in a meeting room.
- Imagine you are in a meeting room and there are different participants.
- Every time you come up with something new, or want to record new information or update information, you write it on the whiteboard.
- In this case, the whiteboard acts as your state and the participants act as a node.
- The state shows us the updated content or information of the entire application.
- Let us move on to the node, another fundamental element in Langraph.
- Nodes are individual functions or operations that perform specific tasks within the graph.
- Each node receives an input, which is often just the current state of your application.
- It processes that input and then produces an output or an updated state.
- A good analogy is an assembly line station.
- Each station does one specific job.
- It could be attaching a part, painting it, inspecting quality, and so on.
- The point is that each station represents a node because it does one specific task.
- Before we go into how to connect different nodes together, I think it is important to understand the most important element of them all: the graph.
- It is so important that it is even in the name Langraph.
- The graph is the overarching structure and it maps out how different tasks, also known as nodes, are connected and executed.
- It visually represents the workflow, showing the sequence and the conditional parts between various operations.
- A graph is quite self-explanatory, but you can think of it as a road map.
- On a road map, you can see different routes connecting cities, with intersections offering choices on which path to take next.
- Here is a great image of what a graph is.
- These are the individual nodes, but you will see they are connected somehow.
- That brings us to the next element: edges.
- Edges are the connections between nodes, and they determine the flow of execution.
- They tell the application which node should be executed next after the current one completes its task.
- A good analogy is a train track.
- Think of the train track as an edge, connecting two stations together in a specific direction.
- The train on the track acts as your state.
- The state gets updated from one station to another.
- There is another type of edge called a conditional edge.
- These are specialized connections that decide the next node to be executed based on a specific condition or logic applied to the current state.
- A good analogy is traffic lights.
- Green could mean go one way, red could mean stop, and yellow could mean slow down.
- The condition, in this case the light color, decides the next step.
- You could also think of it as an `if else` statement.
- We move on to the start point.
- The start point, or start node, is a virtual entry point in Langraph and marks where the workflow begins.
- It does not perform any operations itself.
- It serves as the designated starting position for the graph's execution.
- A good analogy is the starting line of a race.
- If you have a start point, you also need an end point.
- The end node signifies the conclusion of the workflow in Langraph.
- When the application reaches this node, the graph's execution completely stops and it indicates that all intended processes have been completed.
- A good analogy is the finish line in a race.
- Now let us look at tools.
- Tools are specialized functions or utilities that nodes can utilize to perform specific tasks.
- For example, a tool could fetch data from an API.
- They enhance the capabilities of nodes by providing additional functionality.
- A common question is what the difference is between a tool and a node.
- A node is part of the graph structure, whereas tools are functionalities used within the nodes.
- A good analogy is tools in a toolbox.
- Imagine a hammer for nails, a screwdriver for screws, and so on.
- The point is that each tool has a distinct purpose.
- You will understand the difference between tools and nodes in much more detail later when we code this, but this is just a general overview.
- Another question you might ask is whether there is a middleman between a tool and a node.
- The short answer is yes.
- That is where a tool node comes in.
- A tool node is a special kind of node whose main job is to run a tool.
- For example, a tool node could be a node whose only job is to use a tool, and that tool fetches data from an API.
- It connects the tool's output back into the state so other nodes can use that information.
- Think of it as an assembly line again.
- Imagine the operator as the tool node, controlling the machine, which is the tool, and then sending the results back into the assembly line.
- If we progress further, let us look at the state graph.
- This is an important element as well.
- It will be one of the first elements you actually interact with.
- Its main purpose is to build and compile the graph structure.
- It manages the nodes, the edges, the overall state, and makes sure the workflow operates in a unified way and that data flows correctly between components.
- You can think of it as a blueprint of a building.
- Just as a blueprint outlines the design and the connections within a building, the state graph defines the structure and the flow of your workflow or application.
- Here is where the runnable comes in.
- Some of you will be coming from a LangChain background, and runnable is quite common there and quite similar in Langraph.
- A runnable in Langraph is the standardized executable component that performs a specific task within an AI workflow.
- It acts as a fundamental building block, allowing us to create modular systems.
- A question you might have is what the difference is between a runnable and a node.
- Short answer: a runnable can represent various operations, whereas a node in Langraph typically receives a state, performs an action on it, and then updates the state.
- A good analogy is a Lego brick.
- Just as Lego bricks can be snapped together to build complicated structures, runnables can be combined to create sophisticated AI workflows.
- Now let us move on to the different types of messages.
- If you come from a LangChain background, you will be familiar with these.
- If not, do not worry.
- We will look at the five most common message types in Langraph.
- The human message represents input from a user.
- The AI message represents responses generated by AI models.
- The system message is used to provide instructions or context to the model.
- The tool message is similar to the function message but specific to tool usage.
- The function message represents the result of a function call.
- If you have used an API like a large language model API before, such as OpenAI's API, many of these will be familiar, especially the system message, the AI message, and the human message.
- That concludes this section.
- I will see you in the next section.
- Now we are about to start coding in Langraph for the very first time.
- We have covered the theory, and now we are actually going to code up some graphs.
- In this subsection, we are going to code our very first graph.
- For this overall section, I have a slight confession to make: we are not going to be building any AI agents here.
- The reason is that we have not really seen how to code in Langraph yet, and combining LLM APIs and tools would be messy and confusing at this stage.
- This course is meant to be beginner friendly, detailed, and comprehensive, and we are going to go step by step.
- Do not worry, we will be coding AI agents soon.
- We are just going to build a couple of graphs first to understand Langraph better, understand the syntax better, and get confident with graph coding.
- The graph we are going to build in this section is called the hello world graph.
- It is the most basic form of graph we can code in Langraph.
- The objectives are to understand and define the agent state structure.
- We will create simple node functions.
- We will process them and update the state.
- We will build the first basic Langraph structure and understand how to compile it, invoke it, and process it.
- The main goal of this section is to understand how data flows through a single node in Langraph.
- The graph we are building has a start point, an end point, and one node sandwiched between them.
- The code begins by importing three main things: `dict`, `TypedDict`, and `StateGraph`.
- `dict` and `TypedDict` are dictionary and typed dictionary, and `StateGraph` is one of the elements we covered earlier.
- You should go back to the previous section if these are unfamiliar.
- You do not need to memorize them.
- Think of `StateGraph` as a framework that helps you design and manage the flow of tasks in your application.
- The first thing we do after importing everything is create the state of our agent, and we can call it `agent_state`.
- The state is a shared data structure that keeps track of all information as the application runs.
- We build the agent state through a class.
- We define `class AgentState(TypedDict)`.
- The state needs to be in the form of a typed dictionary.
- Let us keep this very basic and pass in one input called `message`.
- The data type of `message` is a string.
- That is normal Python.
- After that, we code our very first node.
- A node is a normal Python function.
- Suppose we want to create a greeting message.
- We define `greeting_node`.
- The input type of a node needs to be the state, and the output type also has to be the state.
- The state keeps track of all the information in your application, so it must be passed in and returned as the updated state.
- We pass in `state` and `AgentState`.
- The output is also `AgentState`.
- After doing the mechanics in the function, we return the updated state.
- A very important habit in Langraph is to write docstrings.
- Docstrings matter because they tell your AI agent, when we eventually build agents, what the function actually does.
- In this case, the docstring says this is a simple node that adds a greeting message to the state.
- We refer to the message by accessing `state["message"]`.
- This allows us to update the message part of the state.
- We can set it to something like `hey` plus the current state message, and also add `how is your day going`.
- The last thing to do is return the state.
- That runs without any errors.
- Now we build the graph.
- `StateGraph` is a framework that helps us design and manage the flow of tasks as a graph.
- We create a graph by passing in the state schema, which in this case is `AgentState`.
- We store that in a variable called `graph`.
- Then we add a node to the graph using `graph.add_node`.
- It requires two parameters: the name of the node and the action it will perform.
- The name can be anything sensible, and here we call it `greeter`.
- The action is the function that defines what the node actually does, so we pass in `greeting_node`.
- We now have a `greeter` node added to the graph.
- We still need the start and end points.
- One way to do that is by using `set_entry_point`.
- It takes one parameter, the key.
- The key is the name of the node to which the start node should connect.
- We pass `greeter`.
- Similarly, we use `set_finish_point` and also pass `greeter`.
- Finally, we compile the graph and store it in a variable.
- That ran without any errors.
- One important caution is that compiling without errors does not mean the graph will run successfully.
- Logical errors can still exist.
- A good way to visualize the graph is using IPython.
- The code we use here should match the picture of the graph we saw earlier.
- The only difference is the node name, which is `greeter`.
- To run the graph, we use `invoke`.
- We pass in a message such as `Bob`.
- We store the result in a variable.
- To get the value, we reference the `message` attribute.
- The final answer becomes `hey Bob how is your day going`.
- That is exactly how we set the function to behave.
- The function concatenates the input message with the greeting.
- We could have changed the greeting to anything else.
- That is the full flow of how everything works.
- You should now understand how to build this first hello world graph.
- It is simple, but if you did not understand it fully, I am going to show you the exercise you need to complete.
- The first exercise is to create a personalized compliment agent.
- You should pass in a name such as `Bob`, and output something like `Bob, you are doing an amazing job learning Langraph`.
- The hint is that you need to concatenate the state, not replace it.
- That is very similar to what we just did.
- Once you have completed the exercise, join me when we build the second graph.
- We are now about to build our second graph.
- It is similar to the first graph, except now we will be able to pass multiple inputs.
- The objectives are to build a more complicated agent state.
- We will create a processing node that performs operations on list data.
- We will see how to work with different data types besides strings.
- We will set up the graph that processes, outputs, and computes these results.
- We will invoke the graph with structured inputs and retrieve the outputs.
- The main goal is to learn how to handle multiple inputs.
- The code imports the same items again: `TypedDict` and `StateGraph`.
- This time, `List` is also imported.
- We first implement the state schema using `class AgentState(TypedDict)`.
- The state schema could technically have been named anything.
- I am calling it `AgentState` because that is how I learned it and it tells you what it actually is.
- The main goal of this graph is to handle and process multiple different inputs.
- That is done in the state.
- We define multiple keys now.
- One input is `values`, which will be a list of integers.
- Another input is `name`, which will be a string.
- Another is `result`, which will also be a string.
- We are now working with a list of integers and a string.
- The state handles three inputs: `values`, `name`, and `result`.
- We then create our node function called `process_values`.
- It still needs the state as input and must return the updated state.
- We keep the habit of writing a docstring.
- The function `process_values` handles multiple different inputs.
- We take the list of integers and sum them.
- We also concatenate the name and store it in the result.
- We access `state["result"]`.
- The string becomes something like `hi there` plus the name, and `your sum is equal to` plus `sum(state["values"])`.
- We return the state.
- We then create the graph using `StateGraph` and pass in `AgentState`.
- We add our node with `graph.add_node`.
- The node name is `processor`.
- The action is `process_values`.
- We set the entry point to `processor`.
- We set the finish point to `processor`.
- We compile the graph.
- When invoking the graph, we use the compiled app.
- A common mistake is to try to use `graph.invoke` instead of the compiled app.
- You need to store the compiled graph in a variable and invoke that.
- If you do not compile the graph, `graph.invoke` will not make sense.
- We invoke it with values such as `[1, 2, 3, 4]` and the name `Steve`.
- Printing the result shows the values, the name, and the result.
- The result becomes `hi there Steve your sum is equal to 10`.
- That is because the function concatenates the name and the sum of the values.
- If we only wanted `result`, we could specify that directly.
- To build your understanding even more, we add print statements before and after the action.
- The print statements show how the state gets updated.
- The input values are `1, 2, 3, 4`, and the name is `Steve`.
- We do not pass `result` as an input.
- Langraph automatically sets that as a `None` value if it is not passed.
- Be careful here.
- If we tried to use `state["result"]` to update itself or to assign something else from it, there would be a problem because `result` would have been initialized as `None`.
- In this case, it worked because we only assigned `result`; we did not use it to assign something.
- The state after the action shows the concatenated result.
- The exercise for this graph is to create a graph that passes in a single list of integers along with a name and an operation.
- If the operation is addition, you add the elements.
- If the operation is multiplication, you multiply all the elements.
- Do this all within the same node.
- For example, the input could be `Jack Sparrow`, values `[1, 2, 3, 4]`, and operation `multiplication`.
- The output should be in the format `Hi Jack Sparrow, your answer is 24`.
- You will need an `if` statement in your node.
- Once you have completed the exercise, we move on to the third graph.
- Welcome to your third graph.
- We are going to make the graph more complicated by building a sequential graph.
- That means we will create and handle multiple nodes that can sequentially process and update different parts of the state.
- We will learn how to connect nodes together through edges.
- We will invoke the graph and see how the state changes step by step.
- The main goal is to understand how to create and handle multiple nodes in Langraph.
- We begin coding the third graph.
- The imports are the same: `StateGraph` and `TypedDict`.
- As in the previous two graphs, we code the state schema first.
- We define `class AgentState(TypedDict)`.
- In this case, all three attributes are strings: `name`, `age`, and `final`.
- We are about to build two node functions, which are again the actions.
- First, we define `first_node`.
- It takes the state and returns the updated state.
- The docstring says this is the first node of our sequence.
- We manipulate the `final` part of the state.
- We set `state["final"]` to an f-string like `hi there` plus the name.
- We return the state.
- Then we create a second node.
- The docstring says this is the second node.
- In this node, we set `state["final"]` to `you are` plus the age plus `years old`.
- There is one logical error in the code, and it is deliberate.
- The problem is that we completely replaced the content we had before.
- We had `hi Charlie`, but we replaced it with `you are age years old`.
- We want both pieces of information.
- The way to fix that is to concatenate and preserve the previous content.
- We can use `state["final"] + state["final"]` or otherwise append to preserve what we had before.
- That solves the logical error.
- To build the graph, we initialize it with `StateGraph(AgentState)`.
- We add the nodes.
- For simplicity, I use the same names as the functions.
- We set the entry point to `first_node`.
- We connect the first node and the second node together using an edge.
- This is the new thing being learned here.
- The edge is added with `graph.add_edge`.
- It uses a start key and an end key.
- The flow goes from the first node to the second node.
- We set the finish point to `second_node`.
- We compile the graph.
- The graph should look like a start point, then the first node, then the second node, and then the end point.
- The flow of the state is from the first node to the second node.
- We invoke the graph with `Charlie` and `20`.
- The output becomes `hi Charlie you are 20 years old`.
- We could have performed all of this in one node, as in the previous subsection, but the goal was to create multiple nodes and handle how the state progresses.
- One important concept learned here is that you can change keys in the state at any point in time as many times as you want.
- Another important lesson is that people often accidentally replace content in an attribute, which leads to logical errors.
- Always be mindful of that.
- The exercise for this graph is to build on what we covered and create three nodes in sequence.
- You need to accept the user's name, their age, and a list of their skills.
- The first node should personalize the name field with a greeting.
- The second node should describe the user's age.
- The third node should list all of the user's skills in a formatted string.
- You then need to combine this and store it in a result field and output that.
- The format should be something like `Linda, welcome to the system. You are 31 years old and you have skills in Python, machine learning, and Langraph`.
- The hint is that you need to use `add_edge` twice.
- This will solidify your understanding of how to build graphs in general.
- Welcome to the fourth graph.
- We are about to learn how to build a conditional graph.
- For the first time, we are going to implement conditional logic in the overall graph structure.
- We will use the start and end nodes to manage entry and exit points.
- We will design multiple nodes to perform different operations, such as addition and subtraction.
- We will create a router node to handle decisions and control the graph flow.
- The main goal is to learn how to use the inbuilt function that allows you to create conditional edges in Langraph.
- The imports are slightly modified this time.
- `TypedDict` and `StateGraph` are still there.
- I also import `START` and `END`.
- There are multiple ways to initialize the start and end points.
- This is another way, and arguably the easier way.
- I do not have a strong preference, but I will teach both ways.
- As standard procedure, we code the schema first.
- We define `class AgentState(TypedDict)`.
- In this case, I want to pass in two numbers and an operation, either plus or minus.
- I could have handled this in one node, but that is not the point here.
- The main concept is conditional logic.
- The state includes `number1`, `operation`, `number2`, and `final_number`.
- `final_number` will be the result of either adding or subtracting the two numbers.
- We create the first node function, `adder`.
- It takes the state schema and returns the updated state schema.
- The docstring says this node adds the two numbers.
- We set `state["final_number"]` to `state["number1"] + state["number2"]`.
- We return the state.
- We create another node, `subtractor`.
- This node subtracts the two numbers.
- The main aim here is not the inner workings of the subtraction, but the conditional logic.
- We then create another kind of node called `decide_next_node`.
- This name makes it clear what it does.
- The docstring says this node selects the next phase of the graph.
- We use an `if` statement to decide which path to take.
- This node will be at the start of the graph.
- The router node decides whether we add or subtract based on the `operation` field.
- If `state["operation"]` is `plus`, we return the edge name `addition_operation`.
- If it is `minus`, we return `subtraction_operation`.
- Those edge names will be defined later.
- The graph is then created with `StateGraph(AgentState)`.
- We add the router node.
- There is a subtle reason this setup will not work as written.
- The issue is that the decision node returns an edge rather than returning the updated state like the other nodes.
- The fix is to use `lambda state: state`.
- That acts as a pass-through function because we are not changing the state there.
- We are only comparing values, not assigning anything.
- There is a difference between comparison and assignment.
- After that, we add the start edge.
- Using `START` and `END` is another way to initialize the entry and exit points.
- We connect `START` to the router node.
- We add a conditional edge with `graph.add_conditional_edges`.
- The source is the router node.
- The path is the `decide_next_node` function.
- The path map is a dictionary.
- `addition_operation` maps to `adder`.
- `subtraction_operation` maps to `subtractor`.
- We then create two edges to connect `adder` and `subtractor` to `END`.
- We compile the graph.
- The graph should look like a start point, then the router, then either the add node or the subtract node, and then the end point.
- The conditional edge names tell us which direction to go.
- We invoke the graph with `number1 = 10`, `operation = minus`, and `number2 = 5`.
- The final number is `5`.
- The code works.
- We go through the whole graph again.
- We create the schema with `AgentState`.
- We create the three nodes: `adder`, `subtractor`, and `decide_next_node`.
- If the operation is plus, it goes to the addition edge.
- If the operation is subtraction, it goes to the subtraction edge.
- The graph is built with the nodes and conditional edges.
- The exercise for this graph is to make the larger version shown in the diagram.
- You need to input four numbers and two operations.
- You need to output the final results.
- For example, one operation could be `10 - 5 = 5` and another could be `7 + 2 = 9`.
- This exercise is meant to solidify your understanding of conditional edges.
- Welcome to graph five.
- We are now going to learn about looping.
- The objectives are to implement logic involving looping to route data back to nodes.
- We are also going to create a single conditional edge.
- The main goal is to code the looping logic.
- I take a slightly different approach this time.
- I show you the graph we want to build from the beginning.
- In practice, when you make your own AI agentic systems, you need to plan how they work.
- You need to see what nodes and edges are needed, whether conditional edges are needed, and where the start and end points go.
- You can do that with pen and paper or software, but you need some sort of blueprint.
- The graph here has a start and end point, a greeting node, and a random node.
- In the greeting node, the user inputs their name and the output is a simple greeting.
- Then the graph progresses to the random node.
- In the random node, we generate five random numbers.
- This graph would be useless in industry, but it is deliberately simple so you can understand the concept.
- We import the usual items and also `random`.
- We start with `AgentState`.
- We need a `name` key for the greeting node.
- We need a `number` list for the random node.
- We also need a counter so we know when to stop looping.
- The counter is an integer.
- When actually making AI agents, you will not know what attributes you need from the start unless you have planned extremely well.
- Usually you will discover them iteratively through practice.
- We then build the greeting node.
- It takes `AgentState` as input and returns the updated state.
- The docstring is written.
- We update the `name` key to something like `hi there` plus the current name.
- We also initialize the counter variable.
- This is done to make the graph more robust.
- If the user passes a negative initial counter, the graph could otherwise generate too many numbers.
- Setting it to zero wipes out a bad user input and makes the loop behave correctly.
- We return the state.
- We then create the random node.
- The docstring says this generates a random number from 0 to 10.
- The code appends the randomly generated number to the `number` list.
- We increment the counter by one.
- We return the state.
- The looping logic is implemented in a function called `should_continue`.
- This should decide what to do next.
- If the counter is less than five, we return the loop edge.
- Otherwise, we return the exit edge.
- I also add a print statement to keep track of progress.
- It is very helpful to add print statements or breakpoints when coding and debugging.
- The trajectory should start at the greeting node, then move into the random node, and loop there five times.
- After that, the `else` branch returns exit, and the graph goes to the end node.
- The graph is created with the greeting and random nodes.
- The edge from greeting to random is added.
- The conditional edge is added with the random node as the source.
- The routing function is `should_continue`.
- The edges are `loop` and `exit`.
- The loop goes back into the random node.
- The exit goes to the end point.
- The graph is compiled.
- We test the graph by passing a name and an initial counter value.
- The graph prints each loop and generates five random numbers.
- The counter ends at five.
- Resetting the counter to zero makes the loop robust even if the initial counter was negative.
- That is one way to create loops in Langraph.
- With practice, you might find other ways.
- The exercise for this graph is to implement an automatic higher-or-lower game.
- The bounds should be integers from 1 to 20.
- The graph should keep guessing.
- The maximum number of guesses is seven.
- If the guess is correct, it stops.
- Otherwise, it keeps looping until it hits the max limit of seven.
- No human should be in the loop at all.
- Each time a number is guessed, the hint node should say either higher or lower.
- The graph should use that information to guess the next value accordingly.
- The input should include a player name such as `student`.
- The guess list should start empty.
- Attempts should be set to zero.
- The lower and upper bounds should be 1 and 20.
- The bounds should adjust after every guess based on the hint provided by the hint node.
- Once you complete the exercise, you will have reinforced your understanding of loops in Langraph.
- We will then begin AI agents.
- Welcome to a brand new section where we actually start learning about AI agents.
- We finally are upgrading our ability in Langraph.
- We are going to build a lot of AI agents in this section.
- We start with the first agent.
- Technically, it is not really an agent, but I called it that because it sounds cool.
- The first subsection is to build a simple bot.
- The objectives are to define a state variable structure with a list of human message objects.
- We briefly mentioned human messages earlier.
- A human message is a message prompt given by the human, meaning us, to the AI.
- We will initialize the GPT-4o model using LangChain's `ChatOpenAI` library.
- We will send and handle different types of messages.
- We will build and compile the graph of the agent.
- The main goal is to integrate LLMs into our graphs.
- The graph looks very simple, like the first graph we made earlier.
- The functionality will be different because now we are integrating an LLM.
- I have already imported all the necessary libraries.
- I also briefly explain each one so we are on the same level.
- `TypedDict` and `List` have been used before.
- The `human_message` import comes from `langchain_core.messages`.
- We also import `ChatOpenAI` from LangChain's OpenAI integration.
- We import `StateGraph`.
- We load environment variables from the `.env` file.
- Langraph is built on top of LangChain, so it makes sense to leverage LangChain's strengths.
- `.env` is just a file used to store secret values such as API keys or configuration values.
- I have my own file in my folder structure so that my API key is not exposed.
- We need the API key because we are making calls to an external LLM.
- If we were using our own LLM locally, such as through Ollama, then we would not need an API key.
- To load the API key, we use `load_env`.
- We define the state with `class AgentState(TypedDict)`.
- The only attribute is `messages`.
- It is a list of `HumanMessage` objects.
- That is because when we invoke the graph, we are inputting human messages.
- To initialize the large language model, we write `llm = ChatOpenAI`.
- We specify the model as GPT-4o.
- I have used `ChatOpenAI` a lot and also `ChatAnthropic`.
- I prefer `ChatOpenAI` because it is simple to use.
- If cost is a concern, you can use the GPT-4o mini model.
- It is extremely cheap in terms of tokens.
- We define our node through a function.
- The node takes the state and returns the state.
- To call the LLM, we use `invoke`.
- We store the response in a variable.
- We pass `state["messages"]` into `llm.invoke`.
- The state messages are sent to the LLM through the `invoke` method.
- The LLM generates a response from its cloud server through the API and returns the response.
- We print the response and return the state.
- We then create the graph.
- We add a node called `process`.
- We add an edge from the start to the end node.
- We compile the graph.
- We ask the user for input with `input("enter something")`.
- We invoke the agent.
- The first test with `hi` returns a real AI response.
- A second test with `who are you` returns that it is an AI language model created by OpenAI called ChatGPT.
- That confirms the LLM is actually working.
- We then modify the code so that it can run multiple messages like a chatbot.
- We use a `while` loop.
- The loop exits when the user types `exit`.
- The conversation works across multiple turns.
- The issue is that the bot does not remember previous turns.
- If we ask for our name later, it does not know it because there is no memory.
- This is why I call this subsection simple bot.
- It is only the most basic LLM wrapper.
- It shows how to integrate an LLM into a graph.
- We embed the LLM within the function, and the function becomes the node action.
- The next AI system is a chatbot with memory.
- The problem we are trying to fix is that the previous bot does not remember what we said before.
- That happened because separate API calls were made.
- We want to create a chatbot with some sort of memory.
- The objectives are to use different message types, especially `HumanMessage` and `AIMessage`.
- We want to maintain a full conversation history using both message types.
- We will use GPT-4o again.
- We will create a sophisticated conversation loop.
- The main goal is to create a form of memory for the agent.
- We import `AIMessage` and `Union`.
- `Union` was covered earlier in the course.
- The state is defined with `class AgentState(TypedDict)`.
- Last time, the messages were only a list of human messages.
- This time, we want to include AI messages too.
- One naive way is to store separate lists, but there is a better way.
- We use `Union[HumanMessage, AIMessage]`.
- Human messages and AI messages are data types in Langraph and LangChain.
- `Union` allows us to store either human messages or AI messages in the messages key.
- These libraries are great, but you can also make your own AI agentic systems by writing plain Python functions.
- Still, using Langraph is recommended because it gives a good balance of control and convenience.
- We initialize the LLM again with GPT-4o.
- We create our node.
- The node uses the LLM to respond to `state["messages"]`.
- We append an `AIMessage` containing `response.content` to the state messages.
- This extracts only the content from the response and stores it as an AI message.
- We print the response and return the state.
- We then reuse the same graph structure from before.
- The big difference now is the conversation history.
- We initialize `conversation_history`.
- We ask the user for input inside a `while` loop.
- The loop exits only if the user types `exit`.
- The conversation history is appended with a human message containing the user input.
- We invoke the agent with the entire conversation history.
- The entire conversation history is sent, not just the current human message.
- The result messages are then used to replace the conversation history.
- We test the bot.
- It remembers the name `Steve` after we say it.
- We add print statements to show the current state.
- The printout shows both human and AI messages in the state.
- The conversation history grows with each turn.
- We then restart the program and see that the state is wiped.
- That happens because the data was only stored in variables.
- A possible solution is to store it in a database, such as a vector database for retrieval-based applications.
- For prototyping, I store it in a text file because it is easy.
- The code writes the conversation log to a file in write mode.
- It iterates through the conversation history.
- For each message, if it is a human message, it writes `You: ...`.
- If it is an AI message, it writes `AI: ...`.
- This preserves the conversation outside the application if it stops.
- The conversation log shows the user messages and AI responses in order.
- The human messages remain unaltered.
- The AI messages are also stored.
- This is a fast and efficient way to store data outside the application.
- It is not the most robust way, but it works well for prototyping.
- The second problem is that the conversation history keeps growing.
- That is a problem because large language models use tokens, and more tokens means more cost.
- We want to be a bit conservative with cost.
- One easy solution is to remove the first human message once the number of human messages exceeds five.
- We remove the earliest message rather than the latest because the latest is more likely to still be relevant.
- That gives us a chatbot with memory that works reasonably well.
- The next AI agent is a React agent.
- React stands for reasoning and acting.
- This is a common type of AI agent in Langraph.
- The graph has a start point, an end point, an agent, and a loop connected to tools.
- The agent can decide which tool to select.
- It also decides when there is no more tool calling to do.
- When that happens, it goes to the end.
- The objectives are to learn how to create tools in Langraph, build a React graph, work with tool messages, system messages, and base messages, and test robustness.
- We import `Annotated`, `Sequence`, and `TypedDict`.
- `Annotated` is a type annotation that provides additional context without affecting the type itself.
- For example, an email can still be a string, but with extra metadata saying it has to be in a valid email format.
- You can inspect that metadata separately.
- `Sequence` is another type annotation.
- It automatically handles state updates for sequences, such as adding new messages to a chat history.
- It avoids manual list manipulation in graph nodes.
- We also import `load_env`.
- We import `BaseMessage`, `ToolMessage`, and `SystemMessage`.
- `ToolMessage` is the message type used after a tool has been called.
- `SystemMessage` provides instructions to the LLM.
- `BaseMessage` is the foundational parent class for all message types.
- `AIMessage`, `HumanMessage`, `ToolMessage`, `SystemMessage`, and other message types inherit from it.
- We also import `ChatOpenAI`, `StateGraph`, `END`, `START`, `tool`, and `ToolNode`.
- We import `add_messages` from `langgraph.graph.message`.
- `add_messages` is a reducer function.
- A reducer function controls how updates from nodes are combined with the existing state.
- Without a reducer function, updates would overwrite the existing state.
- With `add_messages`, updates are appended instead of replacing previous messages.
- We define the state with `class AgentState(TypedDict)`.
- The only key is `messages`.
- It uses `Sequence[BaseMessage]` and the `add_messages` reducer to preserve state.
- We define our first tool using a decorator.
- The tool is a simple addition tool.
- It takes `a` and `b` as integers and returns their sum.
- The docstring matters because the LLM uses it to understand the tool.
- We create a list of tools.
- Then we create the model.
- We bind the tools to the model using `bind_tools`.
- That gives the LLM access to the tools.
- We define the agent node function `model_call`.
- The LLM is invoked with a system message.
- The system message says the model should answer the query to the best of its ability.
- We can also store the system prompt as a `SystemMessage` instead of a plain string.
- That is more readable.
- The response is returned as updated messages.
- We include the human query by passing `state["messages"]`.
- We define the conditional edge function `should_continue`.
- It checks whether there are any more tool calls in the last message.
- If there are, it continues.
- If not, it ends.
- The graph is created.
- The agent node is added.
- The tool node is added.
- The entry point is set to the agent.
- The conditional edge is added so the graph can decide whether to go to the tools or to the end.
- We also add a normal edge from the tool node back to the agent.
- That creates the loop.
- We compile the graph.
- A helper function is used to display the tool-calling flow more cleanly.
- We test the graph by asking it to add `3 + 4`.
- It calls the `add` tool and returns `7`.
- We test it with `34 + 21`, and it returns `55`.
- The docstring is necessary; without it, the tool will not work if no description is provided.
- We then test multiple tool calls.
- We ask it to add `3 + 4` and then `12 + 12`.
- It calls the tool multiple times and returns the results.
- We add more tools: `subtract` and `multiply`.
- The only change needed is to include them in the tools list.
- We test a more complicated input like `40 + 12` and then multiply the result by `6`.
- The agent uses the `add` tool and then the `multiply` tool.
- The final answer is `312`.
- The tool calls show that the LLM is not relying on its own internal arithmetic.
- The LLM decides which arguments to pass to each tool.
- We then add a joke request like `also tell me a joke please`.
- The agent still works and returns the arithmetic result plus a joke.
- That shows how robust the React agent is.
- The next AI agent is a mini project called Drafter.
- Picture a company where the boss says the team is spending too much time drafting documents and emails.
- The goal is to create an AI agentic system that speeds up drafting.
- The system should support human-AI collaboration, continuous feedback, and saving drafts.
- The graph looks similar to a React agent, but one of the tools is a save tool, and once that tool is used, the process should end.
- That is why this structure differs from a standard React agent.
- We define a global variable.
- The reason is that the proper Langraph way would be to use injected state, but that is beyond the scope of this course.
- The workaround is to use a global variable that the tools can update.
- The save tool can then use that global content and write it into a text file.
- We define the agent state with `messages`.
- It uses `Annotated[Sequence[BaseMessage], add_messages]`.
- There are two tools: `update` and `save`.
- The `update` tool updates the document with the provided content.
- It uses the global variable to store the current document content.
- It returns a confirmation string saying the document was updated successfully.
- The `save` tool saves the current document to a text file and finishes the process.
- It takes a `file_name` argument.
- The file name is forced to end with `.txt` if it does not already.
- It writes the contents of the global document variable to the text file.
- It also has exception handling for debugging.
- We create a list of tools containing `update` and `save`.
- We bind the tools to the model.
- We define the `r_agent` function.
- It uses a large system prompt.
- The prompt says the model is `Drafter`, a helpful writing assistant.
- It should help the user update and modify documents.
- If the user wants to update, it should use the update tool.
- If the user wants to save, it should use the save tool.
- It should always show the current document after modifications.
- If there are no messages yet, we provide an introduction message such as asking what the user would like to create.
- Otherwise, we ask what they would like to do with the document.
- The user input is wrapped as a human message.
- The system prompt and current messages are combined and sent to the model.
- The response is printed in a cleaner format.
- The updated state is returned.
- The conditional edge function decides whether to continue or end.
- If there are no messages, it continues.
- It also checks the most recent tool message.
- If the save tool was used, the conversation ends.
- If the update tool was used, it continues.
- We create the graph.
- We add the `agent` and `tools` nodes.
- The `tools` node is a `ToolNode`.
- The entry point is set to the agent.
- We add the edge from the agent to the tools.
- We add the conditional edge from the tools back to either the agent or the end.
- We compile the graph.
- A helper function is used to invoke the graph in a compact way.
- We run the program.
- At first, the document is empty.
- We ask it to write an email to Tom saying we cannot make it to the meeting.
- The model drafts an email.
- We then give feedback, such as specifying the meeting time and clarifying details.
- The draft is updated.
- We change the sender name to `V`.
- We refine the wording further and ask it to place the line breaks differently.
- The draft updates again.
- When we say `I like it, save it please`, it uses the save tool.
- The document is saved to a file, and the file contents match the draft.
- We can also use it for short stories or other documents.
- We can start with an empty list or an already existing document.
- If we ask it to write an email, it may ask follow-up questions instead of immediately using a tool.
- That shows how robust the human-AI collaboration is.
- If desired, this project could be extended with voice features, speech-to-text, text-to-speech, a GUI, or a knowledge base.
- The next AI agent is retrieval augmented generation, or RAG.
- The graph looks similar to a React agent, but with a retriever agent and a main LLM agent.
- It has a loop and a conditional edge.
- We also learn a bit about RAG itself.
- I assume you already know what RAG is, so I will not go into all the nitty-gritty details.
- We import the necessary libraries.
- There are four imports we have not seen before, and I explain them as they appear.
- We load the `.env` file.
- The LLM is initialized with a `temperature` parameter.
- `temperature = 0` makes the model more deterministic.
- `temperature = 1` would make it more stochastic.
- We create the embedding model.
- The embedding model converts text into vector embeddings.
- The embedding model must be compatible with the LLM.
- For example, if the embedding model and LLM come from different ecosystems, their vector dimensions may not match.
- The PDF is the stock market performance 2024 document.
- It contains nine pages.
- The code includes an error message in case the PDF path is wrong.
- We use `PyPDFLoader` to load the PDF.
- The `pages` variable contains the loaded pages.
- The PDF has nine pages as expected.
- We then perform chunking.
- The chunk size is `1000`.
- The chunk overlap is `200`.
- Chunking means creating chunks of text after a certain token threshold.
- The overlap means consecutive chunks share some tokens.
- The chunks are produced from all pages of the document.
- We use a Chroma vector database to store the embeddings.
- The collection is called `stock_market`.
- There is a check to see whether the collection already exists.
- If it does not, the collection is created in the specified directory.
- The vector store is created in a `try/except` block.
- The retriever is then created.
- It uses similarity search.
- `k = 5`, meaning five chunks are returned.
- We define a retriever tool.
- It takes a query and returns a string.
- The tool searches the document and returns relevant information.
- It invokes the retriever with the query.
- If no relevant information is found, it returns a message saying so.
- Otherwise, it stores the similar chunks and returns them.
- We bind the tool to the model.
- We define the agent state again using `add_messages`.
- We define `should_continue`.
- It checks whether the last message contains tool calls.
- If it does, the process continues.
- If it does not, it ends.
- We define a large system prompt.
- It says the assistant should answer questions about the loaded document.
- It should cite the specific parts of the document used in the answer.
- This helps reduce hallucinations.
- We create a dictionary of tools.
- We define the LLM agent function, which calls the LLM with the current state and system messages.
- We then create the retriever agent.
- The retriever agent executes the tool calls from the LLM response.
- It checks whether the tool name is valid.
- If the tool name is invalid, it returns an error message asking to retry and choose from the available tools.
- If it is valid, it invokes the tool and returns the results.
- We create the graph.
- We add the two AI agents as nodes.
- We add the conditional edge from the LLM node using `should_continue`.
- We set the entry point.
- We compile the graph.
- We also create a helper function for repeated question answering.
- It loops until the user types `exit` or `quit`.
- We test the graph by asking how the S&P 500 performed in 2024.
- It retrieves the relevant chunks and answers with citations.
- The answer aligns with the source document.
- We ask how OpenAI performed in 2024.
- The retriever finds no relevant information.
- The model correctly says the document does not provide specific information about OpenAI stock performance.
- That confirms the RAG setup is working correctly.
- This is how you create a retrieval augmented generation graph in Langraph.
- That brings us to the end of this course.
- I hope you liked it and learned a lot about Langraph.
- Although the course is ending here, your journey in Langraph is just beginning.
- Think about all the AI projects and AI agent systems you can now make.
- If you have further questions about the course material or just want to say hi, you can message me on LinkedIn.
- Thank you so much for watching this course.
- I hope to see you in another course.
- Take care.