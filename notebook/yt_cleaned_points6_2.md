- The current graph will not work because of a subtle issue in the line add_node router decide_next_node.
- The problem is with decide_next_node: when invoked the dock string appears, but something else is wrong.
- Look closely at the three functions: two of them return an updated state and the node edge, while the decide_next_node node only returns the edge.
- The node that only returns the edge does not return the updated state, and that difference breaks how Langraph expects nodes to behave.
- Langraph requires nodes that change or propagate state to return the state object, not just an edge identifier.
- The fix is to use a pass-through function: lambda state: state.
- The lambda state function simply returns the input state as the output state.
- Think of lambda state as a pass-through function that forwards the same state unchanged.
- Using a pass-through is valid here because the router is only performing comparisons and not assigning or changing any state fields.
- There is a difference between comparison and assignment; we compare to choose an edge but do not assign to state in the router, so passing the same state through is safe.
- After fixing with the pass-through lambda, continue building the graph by adding the normal edge between start and router.
- Instead of set_entry_point and set_finish_point, use the start and end keywords you imported to define start and end nodes.
- Connect the start point to the router node because the router must decide which node (add or subtract) to route to; connecting start directly to add or subtract would bypass the router's purpose.
- The new feature introduced here is graph.add_conditional_edge.
- graph.add_conditional_edge can look more complicated than it is: the first parameter is the source node name.
- The second parameter is the path action function, which in this case is the decide_next_node function.
- The third parameter is the path_map, a dictionary mapping edge names to the node names they point to.
- You can ignore "hashable runnable any" scaffolding for now; focus on source, action (path), and path_map.
- The path_map entries are in the format edge_name: node_name.
- In this example, "addition operation" and "subtraction operation" are edge names; they map to add_node and subtract_node respectively.
- After creating the conditional edge mapping, create edges from add_node to endpoint and from subtract_node to endpoint so both branches end at the same endpoint.
- Compile the graph with app = graph.compile and verify there are no errors.
- Visualize the compiled graph: start -> router -> (addition operation -> add_node) and (subtraction operation -> subtract_node) -> endpoint.
- The edge names "addition operation" and "subtraction operation" are labels used to indicate which path to take to reach add_node or subtract_node.
- To invoke the graph in code, define inputs such as number_one = 10, operation = "minus", number_two = 5 and invoke the app to get final_number = 5.
- The example prints: number_one = 10, operation = "minus", number_two = 5, final_number = 5.
- This invocation example shows another way to invoke a graph compared to previous examples.
- Recap of the simple conditional graph: import libraries; define state schema with AgentState TypedDict; create add_node, subtract_node, and decide_next_node.
- decide_next_node checks operation and returns either the "addition operation" or "subtraction operation" edge.
- Build the graph by adding nodes, adding start->router edge, and adding the conditional edges with the router as source and path_map mapping edge labels to nodes.
- This conditional edges pattern uses the "edge name -> node name" format and can be visualized as addition operation -> add_node and subtraction operation -> subtract_node.
- Understanding conditional edges is tricky at first, so practice with the exercise to solidify comprehension.
- Exercise: implement a larger graph that replicates the structure twice so you can input four numbers and two operations and output two final results.
- The exercise requires inputting number_one, number_two, number_three, number_four and their respective operations to compute two final outputs.
- The example in the transcript shows 10 - 5 = 5 and 7 + 7 + 2 = 9 as expected outputs for the two branches; study the exercise and cross-reference the GitHub solution if needed.
- The exercise is designed to solidify your understanding of conditional edges for future graphs and AI agents.
- Moving on: the final graph of this section will introduce looping logic; objectives are small: implement logic which loops data back to nodes and create a single conditional edge for looping.
- The author encourages completing the previous exercise before doing looping because it will be the hardest exercise to this point.
- Graph 5 will demonstrate looping in Langraph; the author will show the final graph blueprint first to encourage planning before coding.
- Planning a blueprint (nodes, edges, conditional edges, start and end points) is an important industry habit before coding agentic systems.
- The loop graph to build contains a start and endpoint, a greeting node, and a random node with a loop from random back to itself.
- Greeting node: accept a user name input, output a greeting like "hi there {name}", then route to the random node.
- Random node: generate five random numbers by traversing the random node five times via the loop.
- Note: this graph is intentionally simple and somewhat contrived; in production you might implement a for loop in a node instead of looping through the graph.
- For this small example, the graph is kept simple so you can learn the looping concept in Langraph.
- Import random and usual libraries; start by defining AgentState TypedDict.
- AgentState includes keys: name (str), number (list[int]), and counter (int).
- The name key stores the user name for the greeting node.
- The number key stores a list of generated random integers for the random node.
- The counter key tracks how many random numbers have been generated and is used to stop the loop.
- When designing agents, you often will not know all attributes needed up front; iterative development improves your ability to predict necessary fields.
- Implement the greeting_node function: def greeting_node(state: AgentState) -> AgentState with a docstring.
- In greeting_node set state.name to a greeting string such as "hi there {name}" which replaces the raw input name with the greeting form.
- Initialize or reset the counter variable in greeting_node to ensure robustness against arbitrary initial counter inputs.
- Resetting counter to a known starting value avoids cases where a user passes negative or very large counter values that would produce incorrect loop counts.
- After setting greeting content and resetting counter, return state.
- Implement random_node: def random_node(state: AgentState) -> AgentState with a docstring.
- random_node should append a randomly generated integer between 0 and 10 to state.number (e.g., number_list.append(random.randint(0, 10))).
- random_node should increment counter by one using state.counter += 1.
- random_node returns state.
- Implement the looping decision function should_continue: def should_continue(state: AgentState) -> str with a docstring to decide next edge.
- should_continue returns "loop" edge if state.counter < 5 and returns "exit" edge otherwise.
- Add print statements inside should_continue to track progress while debugging; print statements or breakpoints are useful for tracing where code may fail.
- The expected trajectory: start -> greeting node -> random node -> enter random node 5 times via the loop edge (counter 0 to 4), then should_continue returns "exit" and the flow goes to endpoint.
- Build the graph: initialize a GraphAgentGraph, add nodes greeting_node and random_node.
- Add a direct edge from greeting_node to random_node because greeting_node passes execution to random_node.
- Add a conditional edge with source=random_node, action=should_continue, and path_map mapping "loop" to random_node and "exit" to endpoint.
- Set the entry point to start; you do not need to explicitly set exit/finish because end was defined.
- Compile the graph with app = graph.compile and verify compilation succeeds.
- Visualize the final graph to confirm start -> greeting -> random (loop back to random) and exit -> endpoint.
- Example invocation: pass a name and initialize number list and counter to -1 to prove robustness; after running you'll see loop prints for iterations and final random numbers; counter will end at 5.
- Because greeting_node resets counter to zero, even if the initial counter is -100 or -1, the graph generates exactly five numbers.
- This loop-in-graph approach is one practical way to implement loops in Langraph; other patterns exist and may be discovered with practice.
- Exercise for this graph: implement an automatic higher-or-lower guessing game graph.
- The guessing game specifics: secret number is between 1 and 20; the graph must guess automatically (no human-in-the-loop) with a maximum of 7 guesses.
- The hint node should respond "higher" or "lower" after each guess, and the graph must adjust its bounds and next guess accordingly.
- Inputs for the guessing graph example: player name "student", guesses = empty list, attempts = 0, lower_bound = 1, upper_bound = 20 to allow easy parameterization for different ranges.
- Hints: after each guess adjust bounds based on the "higher" or "lower" hint to narrow the search; stop when guess is correct or attempts reach 7.
- Completing this exercise reinforces loops and conditional routing in Langraph; cross-reference the GitHub answer after completing it.
- New section: introduction to AI agents in Langraph; this section builds several agentic systems that integrate LLMs into graphs.
- First agent/subsection goal: build a simple bot that wraps an LLM; objectives include defining a state variable with a list of HumanMessage objects and initializing a ChatOpenAI model via LangChain.
- You will send and handle messages, build and compile the agent graph, and learn how to integrate LLMs into graph nodes.
- Import human message type from langchain messages (HumanMessage).
- Initialize ChatOpenAI model from langchain.chat_models (ChatOpenAI).
- Langraph is built on top of LangChain, so Langraph leverages LangChain libraries for LLM access and messaging abstractions.
- Use an .env file and load it (load_dotenv) to store API keys securely and avoid exposing keys in code.
- An API key is required because external cloud LLM calls are made; if you were using a locally hosted LLM integrated with LangChain, an API key might not be necessary.
- Load environment variables with load_dotenv to retrieve API keys for external LLMs.
- Define AgentState TypedDict where messages: List[HumanMessage] because you will pass human messages to the LLM when invoking it.
- Initialize the LLM with lm = ChatOpenAI(model="gpt-4") or another model string; ChatOpenAI is convenient and integrates with LangChain.
- Create a node function process(state: AgentState) -> AgentState that invokes the LLM using lm.invoke with state.messages as the input.
- lm.invoke expects an input shaped like a language model input; passing state.messages tells the LLM the sequence of messages to process.
- Store the LLM response in a response variable, print the response for debugging, and return state.
- Build the graph with a node named process that uses the process function, add an edge start->process->end, and compile with graph.compile.
- Example usage: prompt user input via input("Enter something: "), append that as a HumanMessage to state.messages, and invoke the compiled agent.
- Running the simple agent: python agentbot.py, input "hi" and the LLM will respond with a natural reply such as "Hello, how can I assist you today?"
- Wrap invocation in a loop to allow multiple messages until the user types "exit".
- Limitation of this simple bot: no memory across separate runs because state is held in runtime variables and not persisted.
- To build memory, augment state.messages to contain both HumanMessage and AIMessage so the model can see conversation history on each call.
- Import AIMessage and use the Union type annotation to allow messages to be List[Union[HumanMessage, AIMessage]].
- Using Union[HumanMessage, AIMessage] allows storing both human and AI messages in the same list, which serves as conversation history.
- Initialize lm again as ChatOpenAI and implement a process node similar to before that invokes lm with state.messages.
- After invocation, append an AIMessage with content equal to response.content to state.messages via state.messages.append(AIMessage(content=response.content)).
- Print the AI response for terminal display, and return state.
- The compiled graph still has the same node structure, but now state.messages contains both human and AI messages and grows as the conversation progresses.
- At runtime, append the latest HumanMessage to conversation_history before invoking the agent so the full conversation history (not just the single prompt) is sent to the LLM.
- After invocation, replace or update conversation_history with the updated state.messages returned by the graph so the history persists inside your running session.
- Demonstration: run python memory_agent.py, input "Hi, my name is Steve", the agent replies "Hi Steve", and subsequent queries like "What is my name?" will reflect memory because the history contains the human introduction and AI response.
- To debug or visualize the conversation state, add print statements inside process to print state.messages before returning state; this shows the list of HumanMessage and AIMessage objects with their content.
- Example printed state: HumanMessage(content="Hi, nice to meet you"), AIMessage(content="Hello, nice to meet you. How can I assist you today?").
- Persisting conversations across process restarts requires saving conversation history externally because variables are ephemeral.
- One simple persistence option for prototypes is writing conversation history to a text file, e.g., logging.txt, by iterating over conversation_history and writing each message with labels "User" and "AI".
- The conversation log code opens logging.txt in write mode, writes metadata, then iterates through each message in conversation_history, checks message type (HumanMessage or AIMessage), and writes message.content to the file.
- Example run that writes to logging.txt demonstrates that the saved file contains the exact human messages as entered (including spelling mistakes) and AI messages as generated.
- A second problem with session memory is unbounded growth of the message list, which increases token usage and API cost over time.
- An easy mitigation is to prune conversation history in code: if number of messages exceeds a threshold (e.g., 5), drop the oldest messages so only recent context remains.
- The threshold (5, 10, 20, etc.) is arbitrary and should balance context retention with cost.
- Summary: with HumanMessage and AIMessage types and appending AI responses to state.messages, you can create a chatbot with short-term memory that persists during a run and optionally persists externally via file or database.
- Next agent type introduced: a React agent, where REACT stands for Reasoning and Acting.
- A React agent topology looks like: start -> agent node -> loop attaching to tools -> agent decides which tool(s) to call and when to stop -> end.
- The React agent must decide which tool to select, issue tool calls, observe tool outputs, and determine when no more tool calls are needed and route to the endpoint.
- Objectives for building a React agent: learn to create tools in Langraph, build a React graph that uses tools, work with tool messages, system messages, base messages, and test robustness of the agent.
- The next coding subsection will implement tools and a full React agent; expect more imports and a longer code section.
- First line of the React agent code imports typing aids: from typing import Annotated, Sequence, TypedDict.
- TypedDict is the familiar typed dictionary for AgentState; Sequence and Annotated are additional type annotations used for stronger typing and metadata.
- Annotated is a type annotation that provides additional context or metadata about a variable without changing its underlying data type.
- A partial example explanation: if you have a TypedDict with an email key, you could annotate the email field to indicate required format or additional context while keeping the type as str.